# AI Scrum Master Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# LiteLLM / AI Model Configuration
# =============================================================================

# The AI model to use (OpenAI format)
LITELLM_MODEL=openai/gpt-4o-mini

# API endpoint for your LLM provider
LITELLM_API_BASE=https://api.openai.com/v1

# API key for LiteLLM
LITELLM_API_KEY=your-api-key-here

# Fallback OpenAI API key (used by LiteLLM)
OPENAI_API_KEY=your-api-key-here

# =============================================================================
# Atlassian Integration (Jira & Confluence)
# =============================================================================

# Your Atlassian domain URLs
JIRA_MCP_URL=https://yourcompany.atlassian.net/rest/api/3
CONFLUENCE_MCP_URL=https://yourcompany.atlassian.net/wiki/rest/api

# Authentication tokens (Basic auth format: "Basic <base64(email:api_token)>")
# Generate: echo -n "your-email@domain.com:ATATT3xFfGF0..." | base64
JIRA_MCP_AUTH_TOKEN=Basic <base64-encoded-email:token>
CONFLUENCE_MCP_AUTH_TOKEN=Basic <base64-encoded-email:token>

# Generic auth tokens (if using different auth method)
MCP_AUTH_TOKEN=
MCP_API_KEY=

# =============================================================================
# Security & SSL Configuration
# =============================================================================

# SSL certificate verification (false for dev, true for production)
MCP_VERIFY_SSL=false

# =============================================================================
# Setup Instructions
# =============================================================================

# 1. Get Atlassian API Token:
#    - Go to: https://id.atlassian.com/manage-profile/security/api-tokens
#    - Click "Create API token"
#    - Copy the generated token
#
# 2. Create Basic Auth Token:
#    - Run: echo -n "your-email@company.com:YOUR_API_TOKEN" | base64
#    - Use result in JIRA_MCP_AUTH_TOKEN and CONFLUENCE_MCP_AUTH_TOKEN
#
# 3. Get LLM API Key:
#    - For OpenAI: https://platform.openai.com/api-keys
#    - For other providers: Check their documentation
#
# 4. Update Atlassian URLs:
#    - Replace "yourcompany" with your actual Atlassian domain
#
# 5. Copy this file to .env:
#    - cp env.example .env
#    - Edit .env with your actual values